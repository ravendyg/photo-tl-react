/// <reference path="../typings/tsd.d.ts" />

export enum Actions {
    ADD_TODO = 0,
    TOGGLE_TODO = 1,
    SET_VISIBITY_FILTER = 2
}

export enum Filters {
    SHOW_ALL = 0,    
    SHOW_ACTIVE = 1,
    SHOW_COMPLETED = 2   
}